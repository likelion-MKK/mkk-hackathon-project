# Administrative shell only. Never pass this file to Docker Compose or Vite.
MIGRATION_DATABASE_URL=postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres

# Restore must target a separate empty verification project/database.
RESTORE_DATABASE_URL=postgresql://postgres:<password>@db.<restore-project-ref>.supabase.co:5432/postgres
