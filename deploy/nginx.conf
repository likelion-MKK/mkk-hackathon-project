map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 8080;
    server_name _;
    client_max_body_size 1m;

    location /media/ {
        alias /usr/share/nginx/html/media/;
        add_header Accept-Ranges bytes always;
    }

    # Reviewed product images. The catalog stores schema-scoped
    # assets/products/<product_id>/<file> paths while the bytes live in the
    # shared media volume, so this prefix must win over the SPA fallback.
    location /assets/products/ {
        alias /usr/share/nginx/html/media/products/;
    }

    location /api/ {
        proxy_pass http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_request_buffering off;
        proxy_buffering off;
    }

    location = /healthz {
        proxy_pass http://api:8000/healthz;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /readyz {
        proxy_pass http://api:8000/readyz;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /vision/ {
        proxy_pass http://vision-gateway:8765;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_read_timeout 1h;
        proxy_send_timeout 1h;
        proxy_buffering off;
    }

    location /manager/ {
        alias /usr/share/nginx/html/manager/;
        try_files $uri $uri/ /manager/index.html;
    }

    location / {
        root /usr/share/nginx/html/kiosk;
        try_files $uri $uri/ /index.html;
    }
}
