[pytest]
# `uv run --project apps/api` keeps the repository root as the working
# directory. Limit that root invocation to the API and shared contract/
# integration suite instead of accidentally collecting vendored artifacts and
# isolated experiment tests.
testpaths =
    apps/api/tests
    tests
pythonpath =
    .
addopts =
    -p no:cacheprovider
